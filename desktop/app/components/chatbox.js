var React = require('react');

var Avatar = require('material-ui/lib/avatar');

var chatbox = React.createClass({
    render: function() {
        return (
            <h1>hello</h1>
        );
    }
});